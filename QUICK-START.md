# 🚀 Quick Start Guide

## ก่อนเริ่ม - ตรวจสอบความพร้อม

```bash
npm run check    # ตรวจสอบว่าติดตั้ง Cloudflared และ Docker แล้ว
```

ถ้ายังไม่ได้ติดตั้ง → ดู [INSTALL.md](INSTALL.md)

---

## ครั้งแรก

### 1. Login (ถ้ายังไม่เคย login)

```bash
npm run login    # Login Cloudflare
```

**หมายเหตุ:** ถ้าเจอ error `existing certificate` แปลว่าคุณ login ไว้แล้ว ให้ข้ามไปขั้นตอนที่ 2 เลย!

### 2. สร้าง Tunnel

```bash
npm run setup    # ตั้งค่า tunnel (ตอบคำถาม)
```

### 3. เริ่มใช้งาน

```bash
npm start        # เริ่มใช้งาน!
```

---

## Setup Wizard (npm run setup)

เมื่อรัน `npm run setup` จะถูกถามคำถาม:

```
Enter tunnel name: app-tunnel
Enter domain: app.sabuytube.xyz
Enter local port: 3000
Enter folder name: app
Continue? yes
```

สคริปต์จะทำให้อัตโนมัติ:
- ✅ สร้าง Cloudflare tunnel
- ✅ คัดลอก credentials
- ✅ สร้าง config.yml
- ✅ ตั้งค่า DNS
- ✅ สร้าง docker-compose file

---

## ใช้งานทุกวัน

```bash
npm start    # เริ่ม
npm stop     # หยุด
```

---

## คำสั่งที่ใช้บ่อย

```bash
npm run status              # เช็คสถานะ
npm run setup               # สร้าง tunnel ใหม่
npm run delete              # ลบ tunnel
npm run tunnel:all:restart  # Restart ทั้งหมด
npm run tunnel:app:logs     # ดู App logs
npm run tunnel:office:logs  # ดู Office logs
```

---

**เท่านี้ก็เสร็จแล้ว!** 🎉

ดูรายละเอียดเพิ่มเติมที่ [README.md](README.md)

