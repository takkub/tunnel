# QA Report: Release v1.1.3 (main HEAD)

**Date:** 2026-08-29
**Tester:** QA Teammate
**Branch / Target:** main HEAD (`9e7df6f`)
**Status:** **PASS**

---

## 1. Executive Summary

All QA verification criteria for release candidate **v1.1.3** have **PASSED**:
1. **QA Gate Full:** `takkub qa-gate` passed with **150/150 tests clean** + TypeScript typecheck clean.
2. **Per-Tunnel Health Badge & API:** `GET /api/tunnels/health` and `GET /api/tunnels/[name]/health` accurately reflect runtime state:
   - Live tunnels with real cloudflared logs showing 4 Registered connections report `connected 4/4` with location tags (`bkk09`, `sin13`, `sin12`, etc.).
   - Stopped / inactive tunnels report `stopped` with `activeConnections: 0` and empty `connections: []`.
3. **Tunnel Log Viewer Modal:** Log modal opens for each tunnel, fetches up to 200 lines (`GET /api/tunnels/[name]/logs?lines=200`), auto-scrolls to bottom, and highlights `ERR` / `FTL` lines with red styling (`.text-red-400`).
4. **Responsive Layouts Across All Pages:** All pages (`/`, `/settings`, `/nginx`, `/docker`, `/setup`) verified across four viewports (**375px**, **768px**, **1280px**, **1600px**) with zero horizontal scrolling (`scrollWidth <= innerWidth`), collapsing cleanly to single-column on mobile and expanding to multi-column grids on wide viewports.
5. **Desktop App Manual Update UI & API:**
   - In standard Web mode (`DESKTOP_MODE` unset), the update check button is hidden, displaying the GitHub Releases link.
   - In Desktop mode (`DESKTOP_MODE=1`), the "ตรวจสอบอัปเดต" button is rendered and clicking it sends `POST /api/desktop/update` (`{ action: 'check' }`), successfully creating `update-request.json` in `TUNNEL_DATA_DIR`.
6. **Security & Loopback Gate:** `403 Forbidden` response is correctly enforced on non-loopback `Host` headers (`Host: example.com`) when running with `DESKTOP_MODE=1` and no `ADMIN_PASSWORD`.

---

## 2. Test Suite & Verification Results

### 2.1 Full QA Gate
- **Command:** `takkub qa-gate`
- **Result:** **PASS**
- **Details:** 150 passed / 0 failed / 0 skipped / duration 9.78s; TypeScript validation clean.
- **Log Report:** `docs/qa/2026-08-29-125911-qa-gate.md`

---

## 3. Detailed Verification Scenarios

### Scenario 1: Per-Tunnel Health Badge & API (`/api/tunnels/health`)
- **Isolation Setup:** Dedicated temporary git worktree (`qa-v113-test`) and test data directory (`qa-v113-data`) with real `.log` files from `%APPDATA%\tunnel-desktop\runtime` (read-only, no credentials copied). Test Next.js server ran on isolated port `8899`.
- **API `GET /api/tunnels/health` Output:**
  - `admin-wash-locker-dev`: `running: true`, `health: "connected"`, `activeConnections: 4`, connections list containing edge locations (`bkk09`, `sin13`, `bkk09`, `sin12`).
  - `demo-game`: `running: true`, `health: "connected"`, `activeConnections: 4`, edge locations (`sin14`, `bkk09`, `sin22`, `bkk09`).
  - `promptpay`: `running: false`, `health: "stopped"`, `activeConnections: 0`, `connections: []`.
  - `stopped-tunnel`: `running: false`, `health: "stopped"`, `activeConnections: 0`, `connections: []`.
- **Single Tunnel Health:** `GET /api/tunnels/admin-wash-locker-dev/health` returned corresponding per-tunnel health object.
- **Verdict:** **PASS**

### Scenario 2: Log Viewer Modal & ERR Highlighting
- **API `GET /api/tunnels/[name]/logs?lines=200`:**
  - Verified log query parameter returns up to 200 tail lines (tested on `promptpay` with 268-line log -> 200 lines returned).
- **Modal Interaction:**
  - Clicked "ดู log" button on `admin-wash-locker-dev`. Modal opened with header `Log — admin-wash-locker-dev`, auto-refresh toggle, refresh, and copy buttons.
  - Verified DOM highlighting: 4 `ERR` lines (`error="Unable to reach the origin service..."`) were correctly styled with `text-red-400`.
- **Verdict:** **PASS**
- **Evidence:** `log-modal.png`

### Scenario 3: Responsive Multi-Column Layout Verification
Verified `document.documentElement.scrollWidth <= window.innerWidth` (no horizontal scroll) across all core pages at 4 standard viewport widths:

| Page | Viewport Width | Columns / Layout | Horizontal Scroll | Result | Evidence |
|---|---|---|---|---|---|
| Dashboard (`/`) | 375px | 1 column | No (`hasHScroll: false`) | PASS | `dashboard-375.png` |
| Dashboard (`/`) | 768px | 2 columns (`md:grid-cols-2`) | No (`hasHScroll: false`) | PASS | `dashboard-768.png` |
| Dashboard (`/`) | 1280px | 3 columns (`xl:grid-cols-3`) | No (`hasHScroll: false`) | PASS | `dashboard-1280.png` |
| Dashboard (`/`) | 1600px | 4 columns (`2xl:grid-cols-4`) | No (`hasHScroll: false`) | PASS | `dashboard-1600.png` |
| Settings (`/settings`) | 375px | 1 column stacked | No (`hasHScroll: false`) | PASS | `settings-375.png` |
| Settings (`/settings`) | 768px | 2 columns (`md:grid-cols-2`) | No (`hasHScroll: false`) | PASS | `settings-768.png` |
| Settings (`/settings`) | 1280px | 3 columns (`xl:grid-cols-3`) | No (`hasHScroll: false`) | PASS | `settings-1280.png` |
| Settings (`/settings`) | 1600px | 3 columns with max-width container | No (`hasHScroll: false`) | PASS | `settings-1600.png` |
| Nginx (`/nginx`) | 375px | 1 column stacked | No (`hasHScroll: false`) | PASS | `nginx-375.png` |
| Nginx (`/nginx`) | 768px | 3-column split (1:2 col spans) | No (`hasHScroll: false`) | PASS | `nginx-768.png` |
| Nginx (`/nginx`) | 1280px | 3-column split (`md:grid-cols-3`) | No (`hasHScroll: false`) | PASS | `nginx-1280.png` |
| Nginx (`/nginx`) | 1600px | 3-column split with max-width container | No (`hasHScroll: false`) | PASS | `nginx-1600.png` |
| Docker (`/docker`) | 375px | 1 column stacked | No (`hasHScroll: false`) | PASS | `docker-375.png` |
| Docker (`/docker`) | 768px | 2 columns (`md:grid-cols-2`) | No (`hasHScroll: false`) | PASS | `docker-768.png` |
| Docker (`/docker`) | 1280px | 3 columns (`xl:grid-cols-3`) | No (`hasHScroll: false`) | PASS | `docker-1280.png` |
| Docker (`/docker`) | 1600px | 3 columns with max-width container | No (`hasHScroll: false`) | PASS | `docker-1600.png` |
| Setup (`/setup`) | 375px | Single column, compact progress | No (`hasHScroll: false`) | PASS | `setup-375.png` |
| Setup (`/setup`) | 768px | Centered container (`max-w-md`) | No (`hasHScroll: false`) | PASS | `setup-768.png` |
| Setup (`/setup`) | 1280px | Centered container (`lg:max-w-2xl`) | No (`hasHScroll: false`) | PASS | `setup-1280.png` |
| Setup (`/setup`) | 1600px | Centered container (`lg:max-w-2xl`) | No (`hasHScroll: false`) | PASS | `setup-1600.png` |

- **Verdict:** **PASS**

### Scenario 4: App Section & Desktop Update UI
- **Desktop Mode (`DESKTOP_MODE=1`):**
  - Rendered "ตรวจสอบอัปเดต" button in the App section on `/settings`.
  - Triggered check via UI button click: `POST /api/desktop/update` was dispatched and wrote `update-request.json` (`{ "action": "check", "at": ... }`) to `TUNNEL_DATA_DIR`.
  - Evidence: `settings-desktop-app.png`.
- **Standard Web Mode (`DESKTOP_MODE` unset):**
  - Update button was hidden; displayed "ดูเวอร์ชันล่าสุดได้ที่ GitHub Releases" linking to `https://github.com/takkub/tunnel/releases`.
  - Evidence: `settings-web-app.png`.
- **Verdict:** **PASS**

### Scenario 5: Security / Non-Loopback Host Gate
- **Request:** `curl.exe -i -H "Host: example.com" http://127.0.0.1:8899/`
- **Response:**
  ```http
  HTTP/1.1 403 Forbidden
  content-type: text/plain;charset=UTF-8

  Forbidden: set ADMIN_PASSWORD before exposing this app via a tunnel
  ```
- **Verdict:** **PASS**

---

## 4. Screenshot Evidence Artifacts

All screenshot artifacts are saved under `$TAKKUB_ARTIFACTS_DIR/screenshots`:

- `dashboard-375.png`, `dashboard-768.png`, `dashboard-1280.png`, `dashboard-1600.png`
- `settings-375.png`, `settings-768.png`, `settings-1280.png`, `settings-1600.png`
- `nginx-375.png`, `nginx-768.png`, `nginx-1280.png`, `nginx-1600.png`
- `docker-375.png`, `docker-768.png`, `docker-1280.png`, `docker-1600.png`
- `setup-375.png`, `setup-768.png`, `setup-1280.png`, `setup-1600.png`
- `log-modal.png`
- `settings-desktop-app.png`
- `settings-web-app.png`

---

## 5. Cleanup Verification
- Test Next.js server processes stopped cleanly.
- Test git worktree `qa-v113-test` and test data directory `qa-v113-data` removed.
- Production environment (port 8888, Tunnel Manager app, cloudflared processes) remained intact and untouched.
