# คู่มือการ Setup Tunnel (อัปเดต)

## การเปลี่ยนแปลงใหม่ ⭐

สคริปต์ `setup-tunnel.js` ได้รับการปรับปรุงให้:

### ✅ ก่อนหน้านี้ (มีปัญหา)
- สร้าง tunnel แล้วคัดลอก JSON file ล่าสุดจาก `.cloudflared`
- อาจได้ tunnel ID เก่า หรือไม่ตรงกับที่สร้างใหม่
- CNAME อาจชี้ไปที่ tunnel ID เก่า

### ✅ ตอนนี้ (แก้ไขแล้ว)
1. **รันคำสั่ง `tunnel create` และรอให้เสร็จสมบูรณ์**
2. **ดึง Tunnel ID จากผลลัพธ์ที่ gen มา** (แบบ real-time)
3. **ใช้ Tunnel ID นั้นไปสร้าง:**
   - คัดลอกไฟล์ `{tunnelId}.json` ที่ถูกต้อง
   - สร้าง `config.yml` ด้วย tunnel ID ที่ถูกต้อง
   - **สร้าง CNAME ที่ชี้ไปที่ `{tunnelId}.cfargotunnel.com`**

## วิธีใช้งาน

```bash
npm run setup
```

จากนั้นตอบคำถาม:
1. **Tunnel name** (เช่น app-tunnel, office-tunnel, tak)
2. **Domain** (เช่น app.sabuytube.xyz)
3. **Local port** (default: 3000)
4. **Folder name** (เช่น app, office, tak)

## ตัวอย่างผลลัพธ์

```
[1/6] Creating Cloudflare Tunnel...
✓ Tunnel ID: b8b6a8f8-e2fa-4c9b-9282-398e82b0a214
✓ Tunnel created with ID: b8b6a8f8-e2fa-4c9b-9282-398e82b0a214

[2/6] Creating config folder...
✓ Created: C:\Users\monch\WebstormProjects\tunnel\tunnels\tak

[3/6] Copying credentials...
✓ Copied: b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.json

[4/6] Copying cert.pem...
✓ Copied: cert.pem

[5/6] Creating config.yml...
✓ Created: config.yml
  Tunnel ID: b8b6a8f8-e2fa-4c9b-9282-398e82b0a214
  Domain: tak.sabuytube.xyz

[6/6] Setting up DNS...
Creating CNAME: tak.sabuytube.xyz -> b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.cfargotunnel.com
✓ DNS route created
  Domain: tak.sabuytube.xyz
  Target: b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.cfargotunnel.com

==================================================
✓ Setup Complete!
==================================================

Tunnel Information:
  Name:      tak
  ID:        b8b6a8f8-e2fa-4c9b-9282-398e82b0a214
  Domain:    tak.sabuytube.xyz
  CNAME:     b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.cfargotunnel.com
  Local:     http://localhost:3000
```

## ข้อดีของการแก้ไข

### 1. **Tunnel ID ถูกต้อง 100%**
- ดึงจาก output ของคำสั่ง `tunnel create` โดยตรง
- ไม่ต้องพึ่งพาการเรียงลำดับไฟล์ใน `.cloudflared`
- ไม่มีโอกาสได้ tunnel ID เก่า

### 2. **CNAME ถูกต้อง**
- สร้างด้วย tunnel ID ที่เพิ่ง gen มา
- ชี้ไปที่ `{tunnelId}.cfargotunnel.com`
- ไม่ซ้ำกับ tunnel เก่า

### 3. **Config ไฟล์ถูกต้อง**
- `config.yml` ใช้ tunnel ID ที่สร้างใหม่
- `credentials-file` ชี้ไปที่ `{tunnelId}.json` ที่ถูกต้อง
- Docker compose ใช้ค่าที่สอดคล้องกัน

### 4. **แสดงข้อมูลชัดเจน**
- แสดง tunnel ID หลังสร้างเสร็จทันที
- แสดง CNAME target ที่จะถูกสร้าง
- Summary แสดงข้อมูลครบถ้วน

## โครงสร้างไฟล์ที่สร้าง

```
tunnels/
  tak/
    b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.json   ← Credentials (ตรงกับ tunnel ID)
    cert.pem                                     ← Certificate
    config.yml                                   ← Config (ใช้ tunnel ID ที่ถูกต้อง)

docker-compose-cloudflare-tak.yml               ← Docker compose file
```

## ตรวจสอบ CNAME บน Cloudflare

1. เข้า **Cloudflare Dashboard**
2. เลือก **Domain** (เช่น sabuytube.xyz)
3. ไปที่ **DNS** → **Records**
4. จะเห็นระเบียน CNAME:
   - **Name:** tak (หรือ subdomain ที่ระบุ)
   - **Target:** `b8b6a8f8-e2fa-4c9b-9282-398e82b0a214.cfargotunnel.com`
   - **Proxy status:** Proxied (🟠 Orange Cloud)

## เริ่มใช้งาน Tunnel

หลังจาก setup เสร็จ:

```bash
# เริ่ม tunnel ด้วย Docker
docker-compose -f docker-compose-cloudflare-tak.yml up -d

# ตรวจสอบสถานะ
docker ps

# ดู logs
docker logs cloudflared-tunnel-tak -f
```

## สิ่งที่แก้ไข (Technical)

1. เปลี่ยน `exec()` function ให้รับ parameter `returnOutput`
2. ดึง output จากคำสั่ง `tunnel create` ด้วย `stdio: 'pipe'`
3. ใช้ regex pattern หา tunnel ID จาก output:
   - `Created tunnel .+ with id ([a-f0-9-]{36})`
   - UUID format: `[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}`
4. ใช้ tunnel ID ที่ได้ในทุก ๆ ขั้นตอน
5. สร้าง DNS route ด้วย tunnel ID แทนชื่อ tunnel

## Troubleshooting

### ถ้า tunnel ID ดึงไม่ได้
สคริปต์จะแสดง output ที่ได้จากคำสั่ง `tunnel create` เพื่อให้ debug

### ถ้า credential file หาไม่เจอ
สคริปต์จะหาไฟล์ JSON ล่าสุดใน `.cloudflared` และใช้แทน แล้วอัปเดต tunnel ID

### ถ้า DNS route สร้างไม่สำเร็จ
สคริปต์จะแสดงวิธีสร้าง CNAME ด้วยตัวเอง:
```
CNAME: {domain} -> {tunnelId}.cfargotunnel.com
```

---

ตอนนี้ไม่มีปัญหา CNAME ซ้ำแล้ว เพราะใช้ tunnel ID ที่ gen มาใหม่ทุกครั้ง! 🎉

