tunnel: 7c3690bd-a0e7-49a4-bbab-ee3f152f3018
credentials-file: /etc/cloudflared/7c3690bd-a0e7-49a4-bbab-ee3f152f3018.json

# Protocol: auto จะลอง QUIC ก่อน แล้ว fallback เป็น HTTP/2 ถ้าไม่ได้
# ถ้าต้องการบังคับใช้ HTTP/2 เปลี่ยนเป็น: protocol: http2
protocol: auto

ingress:
  - hostname: app.sabuytube.xyz
    service: http://host.docker.internal:3000
  - service: http_status:404
